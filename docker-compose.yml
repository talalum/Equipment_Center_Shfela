services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # The data survives even when the container is rebuilt.
      - warehouse-data:/data
    restart: unless-stopped

volumes:
  warehouse-data:
