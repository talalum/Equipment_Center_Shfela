services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # הנתונים שורדים גם כשהקונטיינר נבנה מחדש.
      - warehouse-data:/data
    restart: unless-stopped

volumes:
  warehouse-data:
